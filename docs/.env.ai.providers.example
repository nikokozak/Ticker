# AI provider defaults (TICKER-PROXY)
#
# Copy this file to `.env.ai.providers` (uncommitted) and edit values there.
# Then apply to your proxy deployment via Fly secrets (or your deployment mechanism).
#
# Notes:
# - Ticker should send `model: "default"` and let the proxy resolve these.
# - Defaults below match the prior local dispatch behavior.

OPENAI_DEFAULT_TEXT_MODEL=gpt-4o-mini
OPENAI_DEFAULT_VISION_MODEL=gpt-4o

ANTHROPIC_DEFAULT_TEXT_MODEL=claude-sonnet-4-20250514
ANTHROPIC_DEFAULT_VISION_MODEL=claude-sonnet-4-20250514

PERPLEXITY_DEFAULT_MODEL=sonar
